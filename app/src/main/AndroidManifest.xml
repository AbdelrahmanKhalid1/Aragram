<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aragram">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.FollowersProfiels.FollowersActivity"></activity>
        <activity android:name=".ui.searchprofile.SearchProfile" />
        <activity android:name=".ui.profile.Edit_Profile" />
        <activity android:name=".ui.login.RegisterActivty" />
        <activity
            android:name=".ui.MainActivity"
            android:label="username" />
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>